import Foundation
import AVFoundation

/// Captures microphone input via `AVAudioEngine`'s input node tap.
public final class MicCapture {
    public init() {}

    /// Installs a tap on the input node and starts the engine.
    public func start() throws {
        fatalError("unimplemented")
    }

    /// Removes the tap and stops the engine.
    public func stop() {
        fatalError("unimplemented")
    }
}

import SwiftUI

/// Settings screen (API keys, output directory, default model, Slack webhook).
struct SettingsView: View {
    var body: some View {
        Text("Settings")
            .padding()
    }
}

import SwiftUI

/// UI for assigning human-readable names to diarized speakers.
struct SpeakerRenameView: View {
    var body: some View {
        Text("Rename Speakers")
            .padding()
    }
}

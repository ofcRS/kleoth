import SwiftUI

/// The root menu-bar popover content.
struct MenuView: View {
    @EnvironmentObject var controller: RecordingController

    var body: some View {
        Text("Kleoth")
            .padding()
    }
}
